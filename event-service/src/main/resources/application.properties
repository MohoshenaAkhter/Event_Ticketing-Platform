spring.application.name=event-service

server.port=8083

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/event_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:1234}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

security.jwt.issuer=${JWT_ISSUER:event-ticketing-gateway}
security.jwt.secret=${JWT_SECRET:event-ticketing-development-secret-32}

venue.service.url=${VENUE_SERVICE_URL:http://venue-service:8084}